name: CI for Go Code #Naming the workflow
on: #for event triggering
  push:
    branches: [main]  #add commas for more branches to monitor
  pull_request:
    branches: [main]
    
jobs: # job to execute?
  build: # name of the job
    name: Load OS # name for printing
    runs-on: ubuntu-latest  # specify OS in virtual machine
    steps: 
    - name: Print hello
      run: |  # run terminal commands
        ls
        echo Hello!!
        
    - name: Setup Go
      uses: actions/setup-go@v3.2.0
      with:
        go-version: 1.18
    - name: Check Go Version
      run: go version
      
    - name: Checkout Repo
      uses: actions/checkout@v2.4.2
      
    - name: Check File System
      run: ls
      
    - name: Install dependencies
      run: |
        go mod init calc   # if we use 'go build', can remove go mo init, go mod tidy and go get
        go mod tidy
        go get github.com/franela/goblin
        go test -v
        
      
    
